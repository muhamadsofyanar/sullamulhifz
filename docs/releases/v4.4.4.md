# v4.4.4 — PHPUnit Test Suite Configuration Hotfix

`@phase 4.4.4`

Rilis ini memperbaiki kegagalan GitHub Actions `Tests #61` setelah kompilasi Blade v4.4.3 berhasil.

## Perbaikan

- Menghapus referensi suite `tests/Unit` dari `phpunit.xml` karena direktori tersebut tidak tersedia dan proyek saat ini hanya mempunyai tes Feature.
- Mempertahankan seluruh tes Feature agar benar-benar dijalankan oleh `php artisan test`.

## Dampak

- Tidak ada migration baru.
- Tidak ada perubahan data pengguna, lembaga, media, kredensial, atau environment.
- Tidak ada tes palsu atau direktori kosong yang ditambahkan.

## Gate

Redeploy Coolify hanya dilakukan setelah `php-tests`, `docker-build`, dan `release-docs` lulus.
